# 企业微信客户群聊智能分析系统 - 系统功能架构图

| 文档名称 | 企业微信客户群聊智能分析系统架构图 |
| :--- | :--- |
| 版本号 | V1.0 |
| 创建日期 | 2026-04-17 |
| 文档状态 | 已定稿 |

---

## 1. 系统总体架构图

```mermaid
graph TB
    subgraph 外部系统
        Java[Java后端服务]
        LLM[通义千问LLM API]
    end
    
    subgraph Python分析服务
        API[API接口层<br/>FastAPI]
        Router[路由层<br/>/api/v1/chat/analyze]
        
        subgraph 核心服务层
            Preprocessor[预处理模块]
            Sentiment[情感分析模块]
            Sensitive[敏感词检测模块]
            Summary[摘要生成模块]
            HighFreq[高频词统计模块]
        end
        
        subgraph 基础设施层
            LLMClient[LLM客户端]
            Config[配置管理]
            Logger[日志系统]
        end
    end
    
    Java -->|HTTP POST| Router
    Router --> API
    API --> Preprocessor
    Preprocessor --> Sentiment
    Preprocessor --> Sensitive
    Preprocessor --> Summary
    Preprocessor --> HighFreq
    Sentiment --> LLMClient
    Summary --> LLMClient
    LLMClient -->|API调用| LLM
    Config -.-> API
    Config -.-> LLMClient
    Logger -.-> API
    API -->|JSON响应| Java
```

---

## 2. 系统分层架构图

```mermaid
graph LR
    subgraph 表示层
        A1[REST API接口]
        A2[Swagger文档]
        A3[健康检查]
    end
    
    subgraph 业务逻辑层
        B1[请求验证]
        B2[消息预处理]
        B3[分析调度器]
        B4[结果聚合]
    end
    
    subgraph 分析引擎层
        C1[情感分析引擎]
        C2[敏感词检测引擎]
        C3[摘要生成引擎]
        C4[高频词分析引擎]
    end
    
    subgraph 数据访问层
        D1[LLM客户端]
        D2[配置读取]
        D3[日志记录]
    end
    
    subgraph 外部服务层
        E1[通义千问API]
        E2[环境变量]
    end
    
    A1 --> B1
    A2 --> B1
    A3 --> B1
    B1 --> B2
    B2 --> B3
    B3 --> C1
    B3 --> C2
    B3 --> C3
    B3 --> C4
    C1 --> B4
    C2 --> B4
    C3 --> B4
    C4 --> B4
    B4 --> A1
    C1 --> D1
    C3 --> D1
    D1 --> E1
    D2 --> E2
```

---

## 3. 核心功能模块架构图

```mermaid
graph TB
    subgraph 输入层
        Input[群聊消息数据<br/>Java推送]
    end
    
    subgraph 预处理模块
        P1[消息解析]
        P2[成员信息提取]
        P3[角色推断]
        P4[文本内容提取]
        P5[消息标准化]
    end
    
    subgraph 情感分析模块
        S1[规则匹配引擎]
        S2[负面词库]
        S3[严重负面词库]
        S4[LLM情感分析]
        S5[结果聚合]
    end
    
    subgraph 敏感词检测模块
        SW1[AC自动机构建]
        SW2[多模式匹配]
        SW3[命中统计]
        SW4[详情记录]
    end
    
    subgraph 摘要生成模块
        SM1[消息采样]
        SM2[优先级排序]
        SM3[格式化处理]
        SM4[LLM摘要生成]
    end
    
    subgraph 高频词统计模块
        HF1[jieba分词]
        HF2[停用词过滤]
        HF3[词频统计]
        HF4[来源追溯]
    end
    
    subgraph 输出层
        Output[结构化分析结果<br/>JSON响应]
    end
    
    Input --> P1
    P1 --> P2
    P2 --> P3
    P3 --> P4
    P4 --> P5
    
    P5 --> S1
    S1 --> S2
    S1 --> S3
    S1 -->|规则未命中| S4
    S2 --> S5
    S3 --> S5
    S4 --> S5
    
    P5 --> SW1
    SW1 --> SW2
    SW2 --> SW3
    SW3 --> SW4
    
    P5 --> SM1
    SM1 --> SM2
    SM2 --> SM3
    SM3 --> SM4
    
    P5 --> HF1
    HF1 --> HF2
    HF2 --> HF3
    HF3 --> HF4
    
    S5 --> Output
    SW4 --> Output
    SM4 --> Output
    HF4 --> Output
```

---

## 4. 数据流转架构图

```mermaid
sequenceDiagram
    participant Java as Java后端
    participant API as API层
    participant Pre as 预处理模块
    participant Sent as 情感分析
    participant Sens as 敏感词检测
    participant Sum as 摘要生成
    participant High as 高频词统计
    participant LLM as 通义千问
    
    Java->>API: POST /api/v1/chat/analyze
    API->>API: 请求验证
    API->>Pre: 消息预处理
    Pre->>Pre: 解析消息内容
    Pre->>Pre: 提取成员信息
    Pre->>Pre: 推断发送者角色
    Pre-->>API: 标准化消息列表
    
    par 并行分析
        API->>Sent: 情感分析请求
        Sent->>Sent: 规则匹配
        alt 规则未命中
            Sent->>LLM: 批量情感分析
            LLM-->>Sent: 情感结果
        end
        Sent-->>API: 情感分析结果
        
        API->>Sens: 敏感词检测请求
        Sens->>Sens: AC自动机匹配
        Sens-->>API: 敏感词结果
        
        API->>Sum: 摘要生成请求
        Sum->>Sum: 消息采样
        Sum->>LLM: 生成摘要
        LLM-->>Sum: 摘要文本
        Sum-->>API: 摘要结果
        
        API->>High: 高频词统计请求
        High->>High: jieba分词
        High->>High: 词频统计
        High-->>API: 高频词结果
    end
    
    API->>API: 结果聚合
    API-->>Java: JSON响应
```

---

## 5. 技术组件架构图

```mermaid
graph TB
    subgraph Web框架层
        FastAPI[FastAPI 0.115.6]
        Uvicorn[Uvicorn 0.34.0]
        Pydantic[Pydantic 2.10.3]
    end
    
    subgraph 业务组件层
        Jieba[jieba 0.42.1<br/>中文分词]
        Ahocorasick[pyahocorasick 2.1.0<br/>AC自动机]
        Tenacity[tenacity<br/>重试机制]
        Loguru[loguru 0.7.3<br/>日志系统]
    end
    
    subgraph LLM集成层
        OpenAI[OpenAI SDK<br/>兼容通义千问]
        QwenPlus[qwen-plus<br/>情感分析模型]
        QwenLong[qwen-long<br/>摘要生成模型]
    end
    
    subgraph 配置管理层
        PydanticSettings[pydantic-settings 2.6.0]
        EnvFile[.env环境变量]
    end
    
    FastAPI --> Pydantic
    Uvicorn --> FastAPI
    
    Jieba --> FastAPI
    Ahocorasick --> FastAPI
    Tenacity --> FastAPI
    Loguru --> FastAPI
    
    OpenAI --> FastAPI
    QwenPlus --> OpenAI
    QwenLong --> OpenAI
    
    PydanticSettings --> FastAPI
    EnvFile --> PydanticSettings
```

---

## 6. 部署架构图

```mermaid
graph TB
    subgraph 客户端
        JavaApp[Java应用服务]
    end
    
    subgraph 负载均衡层
        LB[负载均衡器<br/>Nginx/SLB]
    end
    
    subgraph 应用服务层
        Pod1[Pod 1<br/>Python分析服务]
        Pod2[Pod 2<br/>Python分析服务]
        Pod3[Pod N<br/>Python分析服务]
    end
    
    subgraph 配置中心
        Config[配置中心<br/>环境变量]
    end
    
    subgraph 监控层
        Prometheus[Prometheus<br/>指标采集]
        Grafana[Grafana<br/>可视化]
        Logs[日志中心<br/>ELK/Loki]
    end
    
    subgraph 外部服务
        QwenAPI[通义千问API<br/>阿里云]
    end
    
    JavaApp -->|HTTP| LB
    LB --> Pod1
    LB --> Pod2
    LB --> Pod3
    
    Config -.-> Pod1
    Config -.-> Pod2
    Config -.-> Pod3
    
    Pod1 -->|指标| Prometheus
    Pod2 -->|指标| Prometheus
    Pod3 -->|指标| Prometheus
    Prometheus --> Grafana
    
    Pod1 -->|日志| Logs
    Pod2 -->|日志| Logs
    Pod3 -->|日志| Logs
    
    Pod1 -->|API调用| QwenAPI
    Pod2 -->|API调用| QwenAPI
    Pod3 -->|API调用| QwenAPI
```

---

## 7. 模块依赖关系图

```mermaid
graph LR
    subgraph API层
        Main[main.py]
        Analyze[analyze.py]
    end
    
    subgraph 模型层
        Request[request.py]
        Response[response.py]
    end
    
    subgraph 服务层
        Preprocessor[preprocessor.py]
        Sentiment[sentiment.py]
        Sensitive[sensitive.py]
        Summary[summary.py]
        HighFreq[highfreq.py]
    end
    
    subgraph 工具层
        LLMClient[llm_client.py]
    end
    
    subgraph 配置层
        Settings[settings.py]
    end
    
    Main --> Analyze
    Analyze --> Request
    Analyze --> Response
    Analyze --> Preprocessor
    Analyze --> Sentiment
    Analyze --> Sensitive
    Analyze --> Summary
    Analyze --> HighFreq
    
    Preprocessor --> Settings
    Sentiment --> LLMClient
    Sentiment --> Settings
    Sensitive --> Settings
    Summary --> LLMClient
    Summary --> Settings
    HighFreq --> Settings
    
    LLMClient --> Settings
```

---

## 8. 错误处理与降级架构图

```mermaid
graph TB
    subgraph 正常流程
        A1[接收请求]
        A2[预处理]
        A3[情感分析-LLM]
        A4[摘要生成-LLM]
        A5[返回完整结果]
    end
    
    subgraph 降级流程
        B1[LLM调用失败]
        B2[情感分析-规则匹配]
        B3[摘要返回null]
        B4[返回降级结果]
    end
    
    subgraph 错误处理
        C1[参数校验失败]
        C2[返回400错误]
        C3[服务内部错误]
        C4[返回500错误]
    end
    
    A1 --> A2
    A2 --> A3
    A3 --> A4
    A4 --> A5
    
    A3 -->|失败| B1
    B1 --> B2
    A4 -->|失败| B3
    B2 --> B4
    B3 --> B4
    
    A1 -->|参数错误| C1
    C1 --> C2
    A2 -->|异常| C3
    C3 --> C4
```

---

## 9. 安全架构图

```mermaid
graph TB
    subgraph 网络安全
        N1[内网部署]
        N2[防火墙规则]
        N3[仅允许Java服务访问]
    end
    
    subgraph 应用安全
        A1[API Key加密存储]
        A2[敏感词库安全加载]
        A3[请求体大小限制]
        A4[消息数量限制]
    end
    
    subgraph 数据安全
        D1[敏感内容脱敏日志]
        D2[响应内容截断]
        D3[不存储原始消息]
    end
    
    subgraph 监控安全
        M1[访问日志记录]
        M2[异常告警]
        M3[请求追踪ID]
    end
    
    N1 --> A1
    N2 --> A2
    N3 --> A3
    
    A1 --> D1
    A2 --> D2
    A3 --> D3
    A4 --> D3
    
    D1 --> M1
    D2 --> M2
    D3 --> M3
```

---

## 10. 性能优化架构图

```mermaid
graph TB
    subgraph 并发优化
        C1[异步FastAPI]
        C2[ThreadPoolExecutor<br/>情感分析批处理]
        C3[并发控制<br/>Semaphore]
    end
    
    subgraph 缓存优化
        H1[AC自动机复用<br/>敏感词检测]
        H2[LLM客户端单例]
        H3[配置缓存]
    end
    
    subgraph 算法优化
        A1[规则优先<br/>减少LLM调用]
        A2[批量处理<br/>50条/批]
        A3[消息采样<br/>摘要生成]
    end
    
    subgraph 资源优化
        R1[连接池复用]
        R2[内存控制<br/>来源限制5条]
        R3[响应截断<br/>200字符]
    end
    
    C1 --> A1
    C2 --> A2
    C3 --> R1
    
    H1 --> A1
    H2 --> R1
    H3 --> R2
    
    A1 --> R3
    A2 --> R2
    A3 --> R2
```

---

## 架构说明

### 核心设计原则

1. **分层架构**：API层、服务层、模型层、工具层职责清晰
2. **模块化设计**：各分析模块独立，可单独启用/禁用
3. **异步处理**：使用FastAPI异步特性提高并发性能
4. **降级策略**：LLM不可用时自动降级到规则引擎
5. **可扩展性**：支持水平扩展，无状态设计

### 关键技术点

| 技术点 | 实现方案 |
| :--- | :--- |
| 并发处理 | ThreadPoolExecutor + Semaphore |
| 敏感词匹配 | AC自动机（pyahocorasick） |
| 中文分词 | jieba + 自定义词典 |
| LLM调用 | OpenAI SDK兼容通义千问 |
| 重试机制 | tenacity指数退避 |
| 配置管理 | pydantic-settings环境变量 |

### 性能指标

| 指标 | 目标值 |
| :--- | :--- |
| 100条消息分析时间 | < 5秒 |
| 500条消息分析时间 | < 15秒 |
| 并发请求支持 | 10 req/min |
| LLM调用成功率 | > 95% |
| 服务可用性 | > 99.5% |
