# 小批量测试：1 个群、几条消息

test-small.json 中是小批量的请求数据，包含 1 个群、几条消息。

执行进行小批量测试：
curl -X POST "http://127.0.0.1:8000/api/v1/chat/batch-analyze" \
  -H "Content-Type: application/json" \
  --data @test-small.json \
  -o result-small.json \
  -w "\nHTTP状态码: %{http_code}\n总耗时: %{time_total}s\n"

  ---

  # 